For now we've got a simple Server and Client scripts working on TCP.

Client should work finbe on Linux, Win and MacOS.
Server needs testing on Win and MacOS.
Header is waaaay to big, but 64 bytes is not that much of a burden.
